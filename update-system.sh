#!/bin/bash

'If you are on a Debian based Distro, type on terminal'
# update your sistem with all packages and then install them
sudo apt update
sudo apt upgrade

'Other distros'
# update your system with all pakages
sudo apt-get update
sudo apt-get upgrade


